[pytest]
# Put the repo root on sys.path so `import evalops` works under a bare `pytest`
# invocation in CI (plain pytest does not add the cwd the way `python -m pytest` does).
pythonpath = .
testpaths = tests
